# 德州扑克真实浏览器点击验收（2026-09-10）

## 环境与验证边界

- Checkout：`C:\Users\hr206\.codex\worktrees\3146\pocker agent`
- Branch：`codex/doudizhu-holdem`
- HEAD：`aee9c5107f2f4385a525810f5eaee635bd22b190`，运行内容包含当前未提交修改。
- Python 包来源：本 checkout 的 `src/pocker_agent`（已实际打印路径核实）。
- 后端启动：PowerShell 中设置 `$env:POCKER_AGENT_DATA_DIR='.validation/holdem-real-data'`，然后运行 `.venv\Scripts\python.exe -m uvicorn pocker_agent.api:app --host 127.0.0.1 --port 8765`。
- 前端启动：在 `frontend` 设置 `$env:VITE_API_URL='http://127.0.0.1:8765'`，运行 `npm run dev -- --host 127.0.0.1 --port 5175`。
- 实际浏览器：Edge，`http://127.0.0.1:5175/`，使用 CUA 实际填写字段、点击按钮并读取页面状态；本记录中的牌局不是由 HTTP 脚本代为操作。
- 使用已获授权的测试服务，模型列表获取和连接测试成功，保存模型 `gemini-3.8-flash`。本报告不记录凭据。

## 实际操作及观察

1. 在页面提交两人德州扑克描述：标准52张、每人两张、筹码1000、大小盲10/20、四街下注、过牌/跟注/加注/全下/弃牌、七选五比较与主池边池结算。
2. 模型询问轮数，填写“1轮”；生成“单挑德州扑克”提案，实际点击“确认规则”和“开始试玩”。
3. 第一局实际点击：跟注 → 翻牌过牌 → 转牌过牌 → 河牌过牌。页面分别显示3、4、5张公共牌，最终显示“你赢了”，筹码1020/980，共2000。公共牌为3H、8H、QH、6S、4H；人类手牌7S/JH，对手QD/5D。人类红桃同花高于对手一对Q，与页面赢家一致。
4. 第二局点击“再来一局”，在“加注到（总投入）”中输入100并点击“加注”。页面进入翻牌，两位玩家各剩900，底池200，证明金额通过页面送达后端。
5. 点击“全下”。页面进入转牌，双方筹码0，底池2000。继续点击转牌过牌、河牌过牌后完成结算，页面显示人类2000、电脑0，“你赢了”。
6. 刷新浏览器，页面显示“已恢复上次牌局”，第二局结算结果、公共牌、13个事件和筹码2000/0仍存在。

## 工具化迁移后的复验（2026-09-10）

在迁移后的源代码上重启后端并刷新同一 Edge 页面，重新开局并实际点击：跟注 → 翻牌过牌 → 转牌过牌 → 河牌过牌。页面展示 3、4、5 张公共牌并完成结算；展开“本局事件”后真实看到：

```text
deck.deal
betting_round.act
all_in.check
phase_progress.advance
community_deal.deal
showdown.call
settle_pots.call
```

这证明浏览器动作经过 HTTP 运行时后，实际调用的是 ToolPlan 中声明的工具。完整的模型 → ToolPlan → 源代码 Uvicorn → 牌局动作验收另见 `scripts/validate_holdem_agent_runtime.py` 与 `docs/agent-tool-chain-validation.md`。

## 历史记录中的问题

- 早期版本双方全下后仍显示“过牌、全下、弃牌”，并要求人类手动推进；当前 `AllInTool` 已从下注轮中排除全下玩家，运行时验收覆盖自动发牌、摊牌和结算。
- 早期版本结算后仍显示“行动：轮到你”和英文`completed`；当前页面已显示“本局结束，可以再来一局”，但状态栏仍保留部分通用字段，后续可继续做文案清理。
- Agent询问“支持1至20轮”，本次只验证1轮，不能据此证明多手牌支持。
- 此次仅验证两人牌局，没有证明3至9人、不同筹码全下边池、短额加注重开规则、所有弃牌分支都正确。
- 牌桌仍混有通用试玩区域；可点击走通不等于用户要求的前端重构已经完成。

结论：两条真实浏览器牌局路径及刷新恢复通过；整体开发目标继续保持active，不能据此标记完整德州扑克验收通过。
